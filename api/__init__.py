# ============================================================
# MODULE: api/__init__.py
# PURPOSE: Marks the api directory as a Python package and sets up
#          the Flask application factory used by routes.py.
# PIPELINE STAGE: API (step 4 of 4 — external interface)
# INPUTS: None — package initializer only
# OUTPUTS: Makes routes importable as api.routes
# ============================================================

"""API package exposing the ConstructIQ audit pipeline via Flask routes."""
